import { IncomingMessage, ServerResponse } from 'http';
import { Lithia } from 'lithia/types';

export async function handleRequest(req: IncomingMessage, res: ServerResponse, lithia: Lithia): Promise<void> {}
